#!/bin/sh

set -e

if [ "$1" = "remove" ];
then
    update-alternatives --remove \
        x86_64-linux-gnu_mirclient8driver_conf \
        /usr/lib/x86_64-linux-gnu/mir/client8driver/mesa/ld.so.conf

    ldconfig
fi



exit 0
